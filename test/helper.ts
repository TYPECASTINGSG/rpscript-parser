import c from 'chai';
import 'mocha';
import {NpmModHelper} from '../src/helper/npmMod';;

describe('Helper', () => {
  xit('npm install', function() {
    // NpmModHelper.installNpmModule('rpscript-api-notifiersasas');

    // NpmModHelper.removeNpmModule('rpscript-api-notifier');
  }).timeout(10000);
});